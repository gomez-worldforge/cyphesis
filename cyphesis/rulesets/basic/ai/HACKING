. moves in best order are knights, bishops and towers then queens and kings
. the chess board is made from top left to bottom right (rows and columns)
. grep on the word FIXME to see the fixes, if there are left
. grep on the word NOTE to see (rather) important notices 
. Use slower WFMath for Vectors and Matrices ?
